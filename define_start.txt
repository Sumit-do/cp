#define f(i, a, b) for (int i = a; i < b; i++)
#define v(type) vector<type>
#define fl(i, a, b) for (int i = b - 1; i >= a; i--)
#define F first
#define S second
#define all(x) x.begin(), x.end()
#define itr(it,container) for(auto it:container)
#define ter(a, b, c) ((a) ? (b) : (c))

